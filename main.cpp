#include "Lox.hpp" 
#include "Token.hpp"

int main(int argc, char* argv[])
{
    //Lox firstProg(argc, argv);
    // Token testTok(TokenType::LEFT_PAREN, "x", "hello", 5);
    // std::cout << testTok.toString() << std::endl;
}