#include "Lox.hpp"
#include "ExpressionTypes.hpp"

void testingExpr(){
    // Token testToken1(TokenType::STRING, "test_token_1", "hello", 5);
    // Literal testLiteral1(testToken1);
    // Literal testLiteral2(testToken1);

    // Binary testBinary(testLiteral1, testToken1, testLiteral2);
    // std::cout << testBinary.m_op.getLexeme();
}

int main(int argc, char* argv[])
{
    Lox firstProg(argc, argv);
}